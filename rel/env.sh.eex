#!/bin/sh

# This release is intentionally single-node. Keep remote Erlang control closed.
export RELEASE_DISTRIBUTION=none
export RELEASE_VM_ARGS="$REL_VSN_DIR/vm.args"
export RELEASE_REMOTE_VM_ARGS="$REL_VSN_DIR/remote.vm.args"

# Mix release scripts require a cookie argument even when distribution is off.
# This public placeholder is not an authentication secret and is never used for
# node-to-node communication.
export RELEASE_COOKIE=cluster_murmur_nondistributed

# Do not let inherited VM flag channels bypass nondistributed mode. Preserve
# only the UTF-8 native filename behavior needed by minimal image runtimes.
unset ERL_AFLAGS
unset ERL_FLAGS
unset ERL_ZFLAGS
export ELIXIR_ERL_OPTIONS=+fnu
