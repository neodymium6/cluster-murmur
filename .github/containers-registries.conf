# Keep pinned container tooling independent from runner-provided configuration.
# All workflow image references are fully qualified, so no search registries are
# required.
unqualified-search-registries = []
